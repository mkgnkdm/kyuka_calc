# CHANGELOG — 年休計算機 変更履歴

## 履歴

### 2026-07-14（視覚的差別化）

- 変更内容: 他アプリと一目で見分けられるよう CSS 変数 `--brand`/`--brand-dark` を導入
  （値は既存ボタン色 #1f6f9f/#185a82 を踏襲、見た目の変更なし）、タブのfaviconを🏖️絵文字に、
  見出しに絵文字を追加。レイアウト・機能は変更なし。テスト91件全パス。

### 2026-07-13（v1.1）

- 変更内容:
  1. ローカル実行ポートを 5009 → 5011 に変更（`app.py` / `start.bat` / `stop.ps1` /
     `CLAUDE.md` / `README.md`）。
  2. 基準日方式（一斉付与）を追加（設計: `_ideas/10_portfolio_kyuka_calc.md` §9-2）。
     「付与の方式」を選べるようにし（既定=法定原則）、基準日方式を選ぶと基準日（月・日）を
     入力できる。初回のみ法定どおり、2回目以降は基準日に統一し、法定日からの前倒しを
     結果表に表示する。斉一的取扱いが適法な「前倒しのみ」であることを、
     `company の各付与日 <= 法定の各付与日` の不変条件としてプロパティテストで担保した。
     `rules.json`（法定表・verified 状態）は変更していない。
- 変更したファイル: `app.py`, `leave.py`, `templates/index.html`, `test_app.py`,
  `start.bat`, `stop.ps1`, `CLAUDE.md`, `README.md`
- テスト: 71件全パス（既存34件＋基準日方式の新規37件）＋UXウォークスルー
  （シナリオA: 既存機能に回帰なし／シナリオD: 新機能）検品済み。
- 未了: rules.json は verified 済みのため今回は変更なし。GitHub・PythonAnywhere 公開は引き続き未了。

### 2026-07-13

- 変更内容: 初版を構築（公開ポートフォリオツール第1号）。入社日＋週の所定労働日数から
  年休の付与日・付与日数（通常付与＋比例付与）を計算し、根拠の法定テーブル・出典・注記
  （出勤率8割・繰越時効・基準日方式）を表示する1画面ツール。法定表は rules.json で管理し、
  専門家確認前は「下書き」警告を表示する。DBなし・保存なし・GET計算（結果URL共有可）。
  テスト34件（表の構造検証・境界〔月末入社・うるう年・勤続上限〕・シナリオA〜Cのスモーク）
  ＋UXウォークスルー検品済み。
- 変更したファイル: `app.py`, `leave.py`, `rules.json`, `templates/index.html`, `test_app.py` ほか一式
- 未了: rules.json の法定表の人間確認（確認後 status を verified に）／GitHub・PythonAnywhere 公開
